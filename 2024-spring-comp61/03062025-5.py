global_list = "I'm actually a string, not a list"

class ListHandler:
    def __init__(self, initial_data)
        self.data = initial_data
        self.title = "Handler Title

    def add_item(self, item):
    self.data.appnd(item)
    print("Added " + item)

    def remove_item(self, item):
        if item in data
            data.remove(item)
        else:
            print("Item was not found in the list")

    def pop_item(self):
        self.data.pop("zero")
        print("Popped an item")

def main():
    my_handler = ListHandler([1, 2, 3])
    my_handler.add_item("4")
    my_handler.remove_item(2)
    my_handler.remove_item(7)
    my_handler.pop_item()

    global_list.remove(1)

    my_handler.clear_all()

main()
